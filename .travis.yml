language: go

go:
  - 1.8.x
  - tip

before_install:
  - go get -t -v ./...

script:
  - go test -race -coverprofile=coverage.txt -covermode=atomic ./... || bash scripts/multiple_package_coverage_go_workaround

after_success:
  - bash <(curl -s https://codecov.io/bash)
