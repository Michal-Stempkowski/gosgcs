language: go

go:
  - 1.8.x
  - tip

before_install:
  - go get -t -v ./...

script:
  - go test -race -coverprofile=coverage.txt -covermode=atomic cyk
  - go test -race -coverprofile=coverage.txt -covermode=atomic core

after_success:
  - bash <(curl -s https://codecov.io/bash)
